{
  "name": "real-time-event-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time event pipeline using React, Node.js, Kafka, ClickHouse, MongoDB, and Grafana",
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:logs": "docker compose logs -f",
    "status": "pnpm list --depth -1 -r",
    "test": "pnpm -r --if-present test:unit",
    "test:backend": "pnpm --filter backend test:unit",
    "test:integration": "pnpm --filter backend test:integration"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.20.0",
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
